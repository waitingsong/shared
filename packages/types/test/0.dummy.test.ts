// eslint-disable-next-line import/order
import assert = require('power-assert')


describe('0.dummy', () => {

  describe('should works', () => {
    it('always passed', () => {
      assert(true)
    })
  })

})

